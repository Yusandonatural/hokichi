# サンプルデータ

`emaff_sample.csv` は **実在の農地ではない**。eMAFF農地ナビの公表項目の構成に
合わせて作ったダミーデータで、`tools/hokichi.py` の動作確認に使う。

実データは eMAFF農地ナビ（https://map.maff.go.jp/）から各自ダウンロードすること。
ダウンロードしたCSVは `data/` 直下に置けば `.gitignore` で除外される。
